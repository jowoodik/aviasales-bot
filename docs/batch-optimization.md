# Оптимизация параллельной проверки маршрутов

**Дата реализации:** 2026-02-09

## Проблема

Ранее планировщик проверял маршруты последовательно по пользователям:
```
Для каждого пользователя:
  Для каждого маршрута:
    checkSingleRoute() → генерирует URLs → getPricesFromUrls()
```

AviasalesPricer мог обрабатывать 10 запросов параллельно, но если у пользователя:
- 1 обычный маршрут (1 URL) → использовался 1 из 10 слотов, 9 простаивали
- 2 обычных маршрута → проверялись последовательно, каждый с простоем

## Решение

Реализована batch-проверка: агрегация ВСЕХ маршрутов всех пользователей подписки в один пул и проверка батчем, используя все 10 слотов параллельно.

### Изменения

#### 1. AviasalesPricer.js

**Добавлен новый метод:**
```javascript
async getPricesFromUrlsWithIndividualFilters(urlsWithFilters)
```

- Принимает массив объектов `[{url, airline, baggage, max_stops, max_layover_hours}, ...]`
- Каждый URL проверяется со СВОИМИ фильтрами (критично для точности!)
- Использует ту же логику батчинга и ротации прокси/куков что `getPricesFromUrls()`
- Параллельность: 10 запросов одновременно

**Старый метод `getPricesFromUrls()` сохранен без изменений.**

#### 2. UnifiedMonitor.js

**Добавлены два метода:**

1. `prepareBatchItem(route)` - подготовка маршрута БЕЗ проверки:
   - Генерирует комбинации через `UnifiedRoute.getCombinations()`
   - Формирует URLs для каждой комбинации
   - Возвращает массив `[{url, combination, airline, baggage, max_stops, max_layover_hours}, ...]`

2. `processBatchResults(routeId, route, batchResults)` - обработка результатов:
   - Принимает результаты проверки для одного маршрута
   - Сохраняет в БД: `route_results`, `route_check_stats`, `combination_check_results`, `price_analytics`
   - Обновляет `last_check`
   - Очищает старые результаты

**Старый метод `checkSingleRoute()` сохранен без изменений для обратной совместимости.**

#### 3. scheduler.js

**Добавлена новая функция:**
```javascript
async checkRoutesBySubscriptionBatch(subscriptionType, monitor, notificationService)
```

**Алгоритм:**

1. Получить всех пользователей подписки
2. Собрать все маршруты с метаданными в `routeMetaMap`
3. Сформировать массив `urlsWithFilters` через `prepareBatchItem()`
4. Вызвать `getPricesFromUrlsWithIndividualFilters()` - ОДНА проверка для ВСЕХ
5. Сгруппировать результаты по `routeId`
6. Для каждого маршрута:
   - `processBatchResults()` - сохранение в БД
   - Классификация приоритета
   - Отправка уведомления (та же логика что раньше!)

**Изменена функция `checkRoutesBySubscription()`:**
- Теперь вызывает `checkRoutesBySubscriptionBatch()` вместо последовательной обработки

**Старая функция `checkUserRoutes()` сохранена:**
- Для обратной совместимости
- Для команды `/check` (ручная проверка одного пользователя)
- Экспортируется в `module.exports`

### Критические требования (выполнены)

- ✅ Сохранена логика уведомлений (приоритеты, периодичность)
- ✅ Сохранены все side-effects (БД, аналитика, статистика)
- ✅ Сохранен порядок обработки подписок (admin → plus → free)
- ✅ Обратная совместимость (старые методы не изменены)
- ✅ Индивидуальные фильтры для каждого маршрута (точность!)

## Прирост производительности

### Примеры (все маршруты - обычные):

**Сценарий 1: 10 пользователей × 1 маршрут (10 маршрутов)**
- Раньше: 10 итераций × ~10 сек = **100 секунд**
- Сейчас: 1 батч × ~10 сек = **10 секунд**
- **Прирост: 10x**

**Сценарий 2: 5 пользователей × 2 маршрута (10 маршрутов)**
- Раньше: 10 итераций × ~10 сек = **100 секунд**
- Сейчас: 1 батч × ~10 сек = **10 секунд**
- **Прирост: 10x**

**Сценарий 3: 30 пользователей × 1 маршрут (30 маршрутов)**
- Раньше: 30 итераций × ~10 сек = **300 секунд**
- Сейчас: 3 батча × ~10 сек = **30 секунд**
- **Прирост: 10x**

**Сценарий 4: Смешанный (обычные + гибкие)**
- 20 обычных (1 комбинация) + 2 гибких (20 комбинаций) = 60 URLs
- Раньше: 22 итерации, но гибкие уже используют 10 слотов → ~140 секунд
- Сейчас: 6 батчей × ~10 сек = **60 секунд**
- **Прирост: 2.3x**

### Вывод

Максимальный прирост - когда много маршрутов с малым количеством комбинаций (обычные маршруты). Типичный прирост: **2-10x** в зависимости от структуры маршрутов.

## Тестирование

### План тестирования (задача #5)

1. **Unit-тесты:**
   - `prepareBatchItem()` возвращает правильные метаданные
   - `processBatchResults()` сохраняет все в БД корректно

2. **Integration-тесты:**
   - Проверить батч из 30 маршрутов (3 пользователя × 10 маршрутов)
   - Проверить смешанные маршруты (обычные + гибкие)
   - Проверить разные фильтры (airline, baggage, max_stops)
   - Проверить NO_RESULTS сценарий

3. **Staging:**
   - Запустить на тестовой БД с реальными данными
   - Сравнить результаты с текущей логикой
   - Проверить логи уведомлений
   - Измерить время выполнения

4. **Production rollout:**
   - Начать с подписки admin (меньше пользователей)
   - Мониторить логи и ошибки
   - Постепенно включить plus и free

### Риски и митигация

| Риск | Митигация |
|------|-----------|
| Ошибка в середине батча | Обрабатываем каждый маршрут в try-catch, Promise.allSettled |
| Разные фильтры у маршрутов | ✅ Каждый URL проверяется со СВОИМИ фильтрами |
| Неправильная статистика | Используем ту же логику что в checkSingleRoute |
| Нарушение порядка уведомлений | Сохраняем группировку по пользователям |
| Ломается существующий код | ✅ Старые методы сохранены без изменений |

## Файлы изменены

1. `services/AviasalesPricer.js` - добавлен метод `getPricesFromUrlsWithIndividualFilters()`
2. `services/UnifiedMonitor.js` - добавлены методы `prepareBatchItem()` и `processBatchResults()`
3. `scheduler.js` - добавлена функция `checkRoutesBySubscriptionBatch()`, изменена `checkRoutesBySubscription()`

## Обратная совместимость

- `AviasalesPricer.getPricesFromUrls()` - не изменен
- `UnifiedMonitor.checkSingleRoute()` - не изменен
- `scheduler.checkUserRoutes()` - не изменен, экспортируется для `/check`

## Мониторинг в production

После деплоя следить за:
- ⏱️ Время выполнения проверок (должно сократиться в 2-10x)
- 📊 Количество успешных/неуспешных проверок (не должно измениться)
- 🔔 Количество отправленных уведомлений (не должно измениться)
- ❌ Логи ошибок (не должно увеличиться)
- 💾 Данные в БД (корректность сохранения)

## Дальнейшие улучшения

1. **Адаптивный батчинг:** динамически менять размер батча в зависимости от нагрузки
2. **Приоритизация:** обрабатывать CRITICAL маршруты в первую очередь
3. **Кэширование:** кэшировать результаты для идентичных комбинаций дат
4. **Метрики:** логировать время выполнения батчей для аналитики
