# Интеграция Telegram Payments с ЮKassa

## Обзор

Бот использует Telegram Payments API для автоматической обработки платежей через провайдера ЮKassa.

## Как это работает

### Поток оплаты

```
1. Пользователь нажимает "Оплатить 199 ₽"
   │
   ▼
2. handlePaymentCallback()
   → генерирует уникальный payload: plus_{chatId}_{timestamp}_{random}
   → сохраняет в БД (status: pending)
   → вызывает bot.sendInvoice()
   │
   ▼
3. Telegram показывает встроенную форму оплаты ЮKassa
   │
   ▼
4. pre_checkout_query (обработчик: handlePreCheckoutQuery)
   → проверяет payload в БД
   → обновляет status: pre_checkout
   → отвечает answerPreCheckoutQuery(true)
   │
   ▼
5. Пользователь вводит данные карты, подтверждает оплату
   │
   ▼
6. successful_payment (обработчик: handleSuccessfulPayment)
   → получает charge_id от ЮKassa
   → обновляет status: completed
   → вызывает SubscriptionService.updateSubscription()
   → отправляет подтверждение пользователю
```

## Переменные окружения

```env
# Токен провайдера платежей ЮKassa
# Тестовый: 381764678:TEST:164790
# Боевой: получить в @BotFather → Payments
PAYMENT_TOKEN=381764678:TEST:164790
```

### Получение боевого токена

1. Откройте @BotFather
2. Отправьте `/mybots`
3. Выберите вашего бота
4. Нажмите "Payments"
5. Выберите "ЮKassa"
6. Следуйте инструкциям для подключения

## Структура БД

### Таблица payments

| Поле | Описание |
|------|----------|
| id | PRIMARY KEY |
| chat_id | ID пользователя |
| payload | Уникальный идентификатор счёта |
| subscription_type | Тип подписки (plus) |
| amount | Сумма в копейках |
| currency | Валюта (RUB) |
| status | pending → pre_checkout → completed |
| telegram_payment_charge_id | ID платежа в Telegram |
| provider_payment_charge_id | ID платежа в ЮKassa |
| created_at | Время создания счёта |
| pre_checkout_at | Время pre-checkout |
| completed_at | Время успешной оплаты |

## Тестирование

### Тестовый режим

Используйте тестовый токен: `381764678:TEST:164790`

### Тестовые карты ЮKassa

| Карта | Результат |
|-------|-----------|
| 4100 0000 0000 0010 | Успешная оплата |
| 4100 0000 0000 0077 | Отклонено (недостаточно средств) |
| 4100 0000 0000 0093 | Отклонено (истёк срок карты) |

**CVV:** любые 3 цифры
**Срок:** любая дата в будущем

### Шаги тестирования

1. Установите тестовый `PAYMENT_TOKEN` в `.env`
2. Запустите бота: `node index.js`
3. Отправьте боту `/upgrade`
4. Нажмите "Оплатить 199 ₽"
5. В форме оплаты используйте тестовую карту

### Проверка результата

```sql
-- Проверить записи в payments
SELECT * FROM payments ORDER BY id DESC LIMIT 5;

-- Проверить подписку пользователя
SELECT * FROM user_subscriptions WHERE chat_id = <your_chat_id>;
```

## Обработка ошибок

### Pre-checkout ошибки

Если pre_checkout_query не будет обработан в течение 10 секунд, Telegram отменит платёж.

**Решение:** Обработчик `handlePreCheckoutQuery` должен отвечать максимально быстро.

### Ошибка "Счёт не найден"

Возникает, если payload не найден в БД.

**Причины:**
- База данных была очищена
- Payload был сгенерирован другим экземпляром бота

### Ошибка активации подписки

Если оплата прошла, но подписка не активировалась:

1. Проверьте запись в `payments` — должен быть `status = completed`
2. Проверьте `provider_payment_charge_id` — это ID платежа в ЮKassa для сверки
3. Вручную активируйте подписку:
   ```sql
   UPDATE user_subscriptions
   SET subscription_type = 'plus',
       valid_from = datetime('now'),
       valid_to = date('now', '+30 days')
   WHERE chat_id = <chat_id>;
   ```

## Логирование

Все этапы оплаты логируются в консоль:

```
📤 Отправлен счёт для 123456789, payload: plus_123456789_1234567890_abc123
📥 Pre-checkout от 123456789, payload: plus_123456789_1234567890_abc123
✅ Pre-checkout подтверждён для 123456789
💰 Успешная оплата от 123456789:
   Payload: plus_123456789_1234567890_abc123
   Telegram charge ID: telegram_charge_xxx
   Provider charge ID: yookassa_charge_xxx
   Сумма: 19900 RUB
✅ Подписка Plus активирована для 123456789
```

## Безопасность

1. **Данные карт** не проходят через бота — всё обрабатывается на стороне ЮKassa
2. **Payload** генерируется уникально для каждого счёта
3. **provider_payment_charge_id** сохраняется для возможной сверки с ЮKassa

## FAQ

### Можно ли вернуть деньги?

Возвраты через Telegram Payments не поддерживаются напрямую. Нужно делать возврат через личный кабинет ЮKassa.

### Что если пользователь закрыл форму оплаты?

Счёт останется в статусе `pending`. Пользователь может нажать кнопку оплаты снова — будет создан новый счёт.

### Как продлить подписку?

При повторной оплате `SubscriptionService.updateSubscription()` обновит `valid_from` и `valid_to`, фактически продлив подписку.
