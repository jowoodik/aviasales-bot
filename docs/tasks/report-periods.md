# Модель периодичности оповещений (финальная версия)

## Уровни приоритета с учётом звука

### 🚨 CRITICAL — со звуком, ВСЕГДА (даже если отключены уведомления)

```javascript
const isCritical = (route) => {
  return (
    // Цена в рамках бюджета или ниже
    route.currentPrice <= route.userBudget ||
    
    // Исторический минимум за всё время
    route.currentPrice < route.historicalMin ||
    
    // Супер-скидка (50%+ от средней)
    route.currentPrice <= route.avgPrice * 0.5
  );
};

// Telegram: disable_notification = false (со звуком)
// Отправлять: ВСЕГДА, даже если пользователь отключил уведомления
```

**Почему со звуком:** Это реальная возможность купить дёшево, которая исчезнет через несколько часов.

**Игнорирует настройки:** Даже если пользователь выключил все уведомления, CRITICAL всё равно приходит.

***

### 🔥 HIGH — беззвучно, не чаще 1 раза в 6 часов

```javascript
const isHigh = (route) => {
  return (
    // Близко к бюджету (превышение 10-15%)
    (route.currentPrice > route.userBudget && 
     route.currentPrice <= route.userBudget * 1.15) ||
    
    // Большая скидка от средней (30-50%)
    (route.currentPrice <= route.avgPrice * 0.7 && 
     route.currentPrice > route.avgPrice * 0.5) ||
    
    // Цена упала на 15%+ за 24 часа
    route.priceDropPercent >= 15
  );
};

// Telegram: disable_notification = true (беззвучно)
// Отправлять: только если включены уведомления
```

**Почему беззвучно:** Это хорошая цена, но не критично — пользователь увидит, когда зайдёт в Telegram.

**Уважает настройки:** Если пользователь отключил уведомления → не отправляем.

***

### 📊 MEDIUM — дневной дайджест (беззвучно)

```javascript
const isMedium = (route) => {
  return (
    // Умеренное превышение бюджета (15-30%)
    (route.currentPrice > route.userBudget * 1.15 && 
     route.currentPrice <= route.userBudget * 1.3) ||
    
    // Цена на 15-30% ниже средней
    (route.currentPrice <= route.avgPrice * 0.85 && 
     route.currentPrice > route.avgPrice * 0.7) ||
    
    // Цена упала на 10-15%
    (route.priceDropPercent >= 10 && route.priceDropPercent < 15)
  );
};

// Telegram: disable_notification = true (беззвучно)
// Отправлять: в дайджесте в 10:00, только если включены уведомления
```

**Почему беззвучно:** Информационное обновление, не требует срочных действий.

***

### 🔕 LOW — тихое обновление (только в боте)

```javascript
const isLow = (route) => {
  return (
    // Сильно выше бюджета (30%+)
    route.currentPrice > route.userBudget * 1.3 ||
    
    // Близко к средней или дороже
    route.currentPrice >= route.avgPrice * 0.85 ||
    
    // Изменение меньше 10%
    route.priceDropPercent < 10
  );
};

// Никаких уведомлений
// Только обновление данных в разделе "Мои направления"
```

***

## Логика с учётом настроек пользователя

```javascript
async function processRouteCheck(route, user) {
  const priority = getPriority(route);
  const notificationsEnabled = user.settings.notificationsEnabled;
  
  switch (priority) {
    case 'CRITICAL':
      // ВСЕГДА отправляем, даже если отключены уведомления
      await sendTelegramMessage(route, {
        disable_notification: false, // со звуком
        ignore_user_settings: true    // игнорируем настройки
      });
      break;
      
    case 'HIGH':
      // Только если включены уведомления
      if (notificationsEnabled) {
        const lastNotif = await getLastNotification(route);
        if (Date.now() - lastNotif > 6 * 60 * 60 * 1000) { // 6 часов
          await sendTelegramMessage(route, {
            disable_notification: true  // беззвучно
          });
        }
      }
      break;
      
    case 'MEDIUM':
      // Только если включены уведомления
      if (notificationsEnabled) {
        await addToDailyDigest(route);
      }
      break;
      
    case 'LOW':
      // Всегда тихо обновляем
      await updateInBotSilently(route);
      break;
  }
}
```

***

## Настройки для пользователя

```
⚙️ Настройки уведомлений

🔔 Регулярные проверки:
[🔕 Отключены] [🔔 Включены]

💡 Даже при отключении вы получите уведомление, когда:
• Цена в рамках вашего бюджета
• Исторический минимум цены
• Супер-скидка 50%+

Эти находки исчезают быстро — важно не упустить!

━━━━━━━━━━━━━━━━━━━━━━━

📊 Формат уведомлений:
[Сразу при находке] [Дневной дайджест в 10:00]

🌙 Ночной режим (23:00-08:00):
[Выключен] [Включён] ← откладывать неважные до утра

━━━━━━━━━━━━━━━━━━━━━━━

Текущая подписка: Plus
Проверки каждый час
```

***

## Ночной режим (23:00 - 08:00)

```javascript
const isNightTime = () => {
  const hour = new Date().getHours();
  return hour >= 23 || hour < 8;
};

if (isNightTime() && user.settings.nightMode) {
  if (priority === 'CRITICAL') {
    // Критичные — отправляем даже ночью, но беззвучно
    await sendTelegramMessage(route, {
      disable_notification: true  // ночью беззвучно
    });
  } else {
    // Остальные — откладываем до 08:00
    await scheduleFor08AM(route);
  }
}
```

**Компромисс:** Критичные уведомления ночью приходят беззвучно, чтобы не разбудить, но пользователь увидит их утром.

***

## Различия между Plus и Free

### Plus (платная подписка):
- Проверки **каждый час**
- HIGH уведомления **раз в 3 часа** (вместо 6)
- Дайджест **2 раза в день** (10:00 и 18:00)
- Настройка **частоты для каждого маршрута**

### Free (бесплатная):
- Проверки **каждые 2 часа**
- HIGH уведомления **только в дайджесте**
- Дайджест **1 раз в день** (10:00)
- Лимит: **3 CRITICAL уведомления в день**

```javascript
const getNotificationRules = (user) => {
  if (user.subscription === 'plus') {
    return {
      checkInterval: 60,        // минут
      highInterval: 3,          // часов
      digestTimes: [10, 18],    // часы
      criticalLimit: null       // без лимита
    };
  } else {
    return {
      checkInterval: 120,       // минут
      highInterval: null,       // только дайджест
      digestTimes: [10],        // часы
      criticalLimit: 3          // в день
    };
  }
};
```

***

## Итоговая таблица

| Приоритет | Звук | Игнорирует "Выкл"? | Частота | Plus | Free |
|-----------|------|-------------------|---------|------|------|
| 🚨 CRITICAL | Да (ночью — нет) | ✅ Да | Моментально | Без лимита | Макс. 3/день |
| 🔥 HIGH | Нет | ❌ Нет | Раз в 6ч | Раз в 3ч | Только дайджест |
| 📊 MEDIUM | Нет | ❌ Нет | Дайджест | 2 раза/день | 1 раз/день |
| 🔕 LOW | Нет | — | Тихо | Тихо | Тихо |

***

## Пример кода для Telegram

```javascript
// CRITICAL уведомление
await bot.sendMessage(chatId, message, {
  parse_mode: 'HTML',
  disable_notification: isNightTime() ? true : false,  // ночью беззвучно
  reply_markup: {
    inline_keyboard: [[
      { text: '🔥 КУПИТЬ БИЛЕТ', url: aviasalesUrl }
    ]]
  }
});

// HIGH уведомление
await bot.sendMessage(chatId, message, {
  parse_mode: 'HTML',
  disable_notification: true,  // всегда беззвучно
  reply_markup: {
    inline_keyboard: [[
      { text: 'Смотреть билет', url: aviasalesUrl }
    ]]
  }
});

// MEDIUM (дайджест)
await bot.sendMessage(chatId, digestMessage, {
  parse_mode: 'HTML',
  disable_notification: true  // всегда беззвучно
});
```

***

## Сообщение при отключении уведомлений

```
✅ Уведомления о регулярных проверках отключены

Вы больше не будете получать сообщения при каждой проверке.

Но я всё равно уведомлю вас, когда:
🚨 Цена в рамках вашего бюджета
🚨 Исторический минимум
🚨 Супер-скидка 50%+

Эти находки критичны — они исчезают за 2-6 часов!

💡 Вы можете посмотреть текущие цены в любой момент:
/my_routes

Включить уведомления обратно:
/settings
```

Теперь система уважает выбор пользователя, но не даёт упустить действительно важные находки! 🎯