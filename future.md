1. [x] При выставлении диапазона дат, если я перелимитил с шириной - предложить выставить минимально доступную дату "До", или отправить новые даты а не сбрасывать процесс.
2. [x] Запоминать аэропорты пользователя, что бы выносить их в предложения вместо Нью-Йорка и прочих))) Что бы не искать каждый раз свой не популярный аэропорт.
3. [x] короче, при создании маршрута, нету возможности "отменить" процесс, т.е. я это сделал через повторный вызов /start
4. [x] Если я не знаю из какого именно аэропорта хочу в Москве вылетать и в какой именно аэропорт хочу в Красноярске прилететь, то как быть? Мне бы вот просто найти самый дешёвый билет в целом из любой точки Москвы в Красноярск в определённую дату. Такая же история и со странами - я не знаю в какой город Японии хочу прилететь, мне бы найти в какие города там есть дешёвые билеты.
5. [ ] Есть ли работа со сложными маршрутами? Условно: я хочу улететь из Москвы в Красноярск, оттуда в Нижнеангарск, оттуда в Улан-Удэ, оттуда в Хужир, и уместить это всё в примерные 2 недели. Я в целом по описанию поняла, что наверное так пока не работает, но решила уточнить.
6. [x] На острове Ольхон на Байкале есть аэропорт "Хужир". Авиасейл его знает, а в боте я его, к сожалению, не нашла. Можно ли это пофиксить?
7. [x] Хочется сделать такой финт ушами: а/к Аврора, которая на Ольхон в Хужир летает, выкладывает в продажу билеты когда ей вздумается, и они очень быстро после этого раскупаются. Хочется подписаться на это направление на нужную мне дату, а когда билеты поступят в продажу - ботик мне маякнет, потому что билет найдёт. Получится ли такое провернуть вообще?x
8. [ ] Указывать даты окончания подписки
9. [ ] Слать оповещения об истечении подписки
10. [ ] При поиске маршрутов учитывать дату подписки по количеству маршрутов
11. [x] Уведомлять о том что оповещения о проверка отключены по дефолту и для их включения надо идти в настройки
12. [ ] Есть даты, когда нет рейсов. И было бы здорово, если бы бот предлагал ближайшие свободные после неудачной попытки найти билет. С теми же параметрами, но на 3-4 дня больше или меньше
13. [x] Поиск билетов происходит по маршруру за раз, хотя это надо разделить - всего 10 потоков, они могут быть забиты 10 задачами, не важно какими
14. [ ] Показывать аналитику схожих направлений после создания маршрута
15. [x] В админке история цен маршрута берется не по маршруту а по направлению, это неверно

---

# 📊 МЕТРИКИ И АНАЛИТИКА - План развития

## Текущее состояние (2026-02-09)

### ✅ Логируется (15 событий):
1. `start` - запуск бота (72 в БД)
2. `main_menu` - главное меню (44)
3. `help` - помощь (14)
4. `view_routes` - просмотр списка маршрутов (187)
5. `view_route_detail` - детали маршрута (94)
6. `create_route_start` - начало создания (81)
7. `route_created` - создан маршрут (56)
8. `edit_route` - редактирование (22)
9. `delete_route` - удаление (21)
10. `view_chart` - график цен (10)
11. `view_heatmap` - тепловая карта (14)
12. `settings` - открытие настроек (63)
13. `subscription_info` - инфо о подписке (34)
14. `upgrade_attempt` - попытка апгрейда (9)
15. `payment_success` - успешная оплата
    🔍 Продолжаем поиск • 15:05

Москва → Денпасар (Бали)

Цена: 382 253 ₽
📅 25.02–24.03
👥 4+1

Ваш бюджет: 370 000 ₽ (+3%)


Продолжаю мониторинг 🔎
---

## ⚠️ НЕ логируется - требует внимания

### 🔴 КРИТИЧНЫЕ (монетизация):
- [x] **`affiliate_click`** - клик по партнерской ссылке Aviasales ✅ ВЫПОЛНЕНО (2026-02-09)
  - event_data: `{routeId, resultId, price, origin, destination, departureDate, returnDate}`
  - **Реализация:** callback-кнопки + логирование + API endpoint + dashboard блок
  - **Файлы:** NotificationService.js, routeHandlers.js, index.js, web/server.js, dashboard.js

- [ ] `view_offers` - просмотр лучших 3 предложений
- [ ] `no_offers_shown` - нет предложений для показа

### 🟡 ВЫСОКИЙ приоритет (UX):
- [ ] `pause_route` - пауза маршрута (есть в коде, но не логируется!)
- [ ] `resume_route` - возобновление маршрута
- [ ] `archive_route` - архивирование (=удаление)
- [ ] `check_command` - команда /check
- [ ] `cancel_route_creation` - отмена создания

### 🟢 СРЕДНИЙ приоритет (детализация):
- [ ] `change_timezone` - смена таймзоны
- [ ] `toggle_notifications` - вкл/выкл уведомлений
- [ ] `toggle_night_mode` - ночной режим
- [ ] `edit_budget` - изменение бюджета
- [ ] `edit_dates` - изменение дат
- [ ] `edit_flexibility` - изменение гибкости (fixed ↔ flexible)
- [ ] `edit_transfers` - изменение пересадок
- [ ] `search_airport` - поиск аэропорта
- [ ] `quick_airport_select` - быстрый выбор из популярных

---

## 📊 Новые метрики для Dashboard

### 1. 💰 Монетизация (КРИТИЧНО)
```
Метрики:
- Клики по Aviasales: 1,234
- CTR (клики/уведомления): 15.3%
- Клики на пользователя: 2.5
- Топ-3 направления по кликам
- Конверсия просмотр → клик: 12.5%
```

**SQL:**
```sql
-- Клики по партнерским ссылкам
SELECT COUNT(*) as total_clicks
FROM user_activity_log
WHERE event_type = 'affiliate_click'
  AND created_at >= datetime('now', '-30 days')

-- CTR
SELECT
  (SELECT COUNT(*) FROM user_activity_log
   WHERE event_type = 'affiliate_click' AND created_at >= datetime('now', '-30 days')) * 100.0 /
  (SELECT COUNT(*) FROM notification_log WHERE created_at >= datetime('now', '-30 days')) as ctr

-- Топ направления по кликам
SELECT
  JSON_EXTRACT(event_data, '$.origin') as origin,
  JSON_EXTRACT(event_data, '$.destination') as dest,
  COUNT(*) as clicks
FROM user_activity_log
WHERE event_type = 'affiliate_click'
  AND created_at >= datetime('now', '-30 days')
GROUP BY origin, dest
ORDER BY clicks DESC
LIMIT 5
```

---

### 2. 🎯 Вовлеченность (Engagement)
```
Метрики:
- Среднее сессий/пользователь: 3.2
- Retention D1/D7/D30: 45% / 23% / 12%
- Stickiness (DAU/MAU): 35%
- Активных маршрутов на активного пользователя: 2.1
- Использование /check: 45 раз
```

**SQL:**
```sql
-- Stickiness = DAU/MAU * 100
SELECT
  (SELECT COUNT(DISTINCT chat_id) FROM user_activity_log
   WHERE created_at >= datetime('now', '-1 day')) * 100.0 /
  (SELECT COUNT(DISTINCT chat_id) FROM user_activity_log
   WHERE created_at >= datetime('now', '-30 days')) as stickiness

-- Active routes per active user
SELECT
  COUNT(ur.id) * 1.0 / COUNT(DISTINCT ur.chat_id) as avg_routes
FROM unified_routes ur
WHERE ur.is_paused = 0
  AND ur.is_archived = 0
  AND ur.chat_id IN (
    SELECT DISTINCT chat_id
    FROM user_activity_log
    WHERE created_at >= datetime('now', '-30 days')
  )
```

---

### 3. 📊 Расширенная воронка маршрутов
```
Текущая воронка (неполная):
- Активные пользователи: 100
- Просмотрели маршруты: 80 (80%)
- Начали создание: 50 (62.5%)
- Завершили создание: 35 (70%)

НУЖНО ДОБАВИТЬ промежуточные шаги:
- Выбрали аэропорты: ? (событие не логируется!)
- Выбрали даты: ? (событие не логируется!)
- Указали бюджет: ? (событие не логируется!)
- Отменили создание: ? (событие не логируется!)
- Паузы/возобновления: ? (не логируется!)
- Удаления: ? (логируется как delete_route)
```

Новые event_type:
- `route_step_airports` - выбраны аэропорты
- `route_step_dates` - выбраны даты
- `route_step_budget` - указан бюджет

---

### 4. ⚙️ Воронка настроек (НОВАЯ)
```
- Открыли настройки: 50
- Изменили таймзону: 20 (40%)
- Настроили уведомления: 35 (70%)
- Включили ночной режим: 15 (30%)
```

---

### 5. 📈 Качество сервиса (НОВОЕ)
```
- Среднее время создания маршрута: 2м 15с
- % завершенных vs отмененных: 85% vs 15%
- % маршрутов на паузе: 12%
- Ошибки поиска/проверок: 3.5%
```

**SQL:**
```sql
-- Среднее время создания маршрута
WITH creation_times AS (
  SELECT
    c.chat_id,
    JSON_EXTRACT(c.event_data, '$.routeId') as route_id,
    (julianday(created.created_at) - julianday(c.created_at)) * 86400 as duration_sec
  FROM user_activity_log c
  JOIN user_activity_log created
    ON c.chat_id = created.chat_id
    AND created.event_type = 'route_created'
    AND JSON_EXTRACT(created.event_data, '$.routeId') = JSON_EXTRACT(c.event_data, '$.routeId')
  WHERE c.event_type = 'create_route_start'
    AND c.created_at >= datetime('now', '-30 days')
)
SELECT AVG(duration_sec) as avg_time_sec
FROM creation_times

-- Completion rate
SELECT
  (SELECT COUNT(*) FROM user_activity_log
   WHERE event_type = 'route_created'
   AND created_at >= datetime('now', '-30 days')) * 100.0 /
  (SELECT COUNT(*) FROM user_activity_log
   WHERE event_type = 'create_route_start'
   AND created_at >= datetime('now', '-30 days')) as completion_rate
```

---

### 6. 🔔 Детализация уведомлений
```
- Всего отправлено: 500
  - CRITICAL: 150 (30%)
  - HIGH: 200 (40%)
  - LOW: 150 (30%)
- Конверсия уведомление → клик: 12.5%
```

---

### 7. 📊 Паттерны использования (НОВОЕ)
```
Графики:
- Создание маршрутов по дням недели
- Пиковые часы активности (не проверок!)
- Сезонность (по месяцам)
```

**SQL:**
```sql
-- Создание по дням недели
SELECT
  CASE CAST(strftime('%w', created_at) AS INTEGER)
    WHEN 0 THEN 'Вс' WHEN 1 THEN 'Пн' WHEN 2 THEN 'Вт'
    WHEN 3 THEN 'Ср' WHEN 4 THEN 'Чт' WHEN 5 THEN 'Пт' WHEN 6 THEN 'Сб'
  END as weekday,
  COUNT(*) as count
FROM user_activity_log
WHERE event_type = 'route_created'
  AND created_at >= datetime('now', '-30 days')
GROUP BY strftime('%w', created_at)

-- Пиковые часы АКТИВНОСТИ (не проверок!)
SELECT
  CAST(strftime('%H', created_at) AS INTEGER) as hour,
  COUNT(*) as actions
FROM user_activity_log
WHERE created_at >= datetime('now', '-7 days')
GROUP BY hour
ORDER BY hour
```

---

## 🎯 План внедрения

### Фаза 1: Критичное (эта неделя)
1. Добавить логирование `affiliate_click`
   - Файлы: `services/AviasalesAPI.js`, `services/AviasalesPricer.js`
2. Создать API endpoint `GET /admin/api/monetization-stats`
3. Добавить блок "Монетизация" на dashboard
4. Логировать `pause_route`, `resume_route`, `archive_route`

**Результат:** Понимание монетизации

---

### Фаза 2: Важное (следующая неделя)
5. Расширить воронку маршрутов (промежуточные шаги)
6. Создать endpoint `GET /admin/api/engagement-stats`
7. Добавить блок "Вовлеченность"
8. Логировать детальные настройки

**Результат:** Понимание retention

---

### Фаза 3: Улучшения (следующий месяц)
9. Качество сервиса (время создания, % ошибок)
10. Паттерны использования (графики)
11. Детализация редактирования маршрутов

**Результат:** Оптимизация UX

---

## 📋 Чеклист файлов для изменения

### Backend (логирование):
- [ ] `services/AviasalesAPI.js` - affiliate_click
- [ ] `services/AviasalesPricer.js` - affiliate_click
- [ ] `handlers/routeHandlers.js` - pause, resume, archive, промежуточные шаги, отмены
- [ ] `handlers/settingsHandlers.js` - изменения настроек
- [ ] `index.js` - команда /check (если есть)

### Backend (API):
- [ ] `web/server.js`:
  - `GET /admin/api/monetization-stats`
  - `GET /admin/api/engagement-stats`
  - `GET /admin/api/quality-stats`
  - `GET /admin/api/patterns-stats`

### Frontend:
- [ ] `web/public/js/pages/dashboard.js`:
  - Блок "Монетизация"
  - Блок "Вовлеченность"
  - Расширить воронку маршрутов
  - Блок "Качество сервиса"
  - Блок "Паттерны активности"

---

## 💡 Дополнительные идеи

### Когорты пользователей:
- Power users: >5 маршрутов, >10 кликов/мес
- Active users: 2-5 маршрутов
- Passive users: 1 маршрут
- Churned: не заходили >30д

### Рекомендательная система:
- Показывать популярные направления
- Предлагать гибкие маршруты
- Персонализированные уведомления

### North Star Metric:
Предлагаю: **"Количество активных маршрутов у активных пользователей"**
- Отражает реальную ценность продукта
- Коррелирует с монетизацией
- Легко измерить и оптимизировать