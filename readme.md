# 🤖 Aviasales Telegram Price Monitor Bot

Telegram-бот для автоматического мониторинга цен на авиабилеты через Aviasales.

---

## 📋 Содержание

- [Возможности](#возможности)
- [Для пользователей](#для-пользователей)
- [Для разработчиков](#для-разработчиков)
- [Установка](#установка)
- [Использование](#использование)
- [Архитектура](#архитектура)

---

## ✨ Возможности

### Для пользователей:

- 📋 **Универсальные маршруты** - фиксированные даты или гибкий поиск
- 🔍 **Гибкий поиск** - автоматический поиск лучших комбинаций дат (до 20 комбинаций)
- 🎯 **Билеты в одну сторону** - поддержка маршрутов без обратного билета
- 🔔 **Умные уведомления** - оповещения только о выгодных предложениях
- 🌙 **Тихие часы** - настройка времени, когда бот не беспокоит
- 🌍 **Поддержка таймзон** - корректное отображение времени
- 📊 **Аналитика цен** - графики изменения цен и heatmap
- ⏸️ **Пауза маршрутов** - временное отключение проверки без удаления

### Для разработчиков:

- 🏗️ **Модульная архитектура** - чистое разделение на handlers/services/models
- 💾 **SQLite** - легковесная база данных
- 🤖 **Puppeteer** - парсинг реальных цен с Aviasales
- 📅 **Cron** - автоматическая проверка каждый час
- 🔄 **Миграции** - автоматическая миграция из старой структуры

---

## 📱 Для пользователей

### Как начать работу

1. Найдите бота в Telegram: `@your_bot_name`
2. Отправьте `/start`
3. Настройте таймзону (опционально)
4. Создайте первый маршрут!

### Создание маршрута

#### Шаг 1: Выберите направление
- Откуда летите (код аэропорта, например SVX)
- Куда летите (код аэропорта, например DXB)

#### Шаг 2: Тип поиска
- **Конкретная дата** - вы знаете точные даты вылета/возврата
- **Диапазон дат** - бот найдет лучшие комбинации в диапазоне

#### Шаг 3: Обратный билет
- Туда-обратно
- В одну сторону

#### Шаг 4: Настройка фильтров
- Количество пассажиров (взрослые, дети)
- Авиакомпания (конкретная или любая)
- Багаж (20 кг или только ручная кладь)
- Пересадки (0, 1, 2 или любое количество)
- Максимальное время пересадки

#### Шаг 5: Пороговая цена
- Укажите максимальную цену для уведомлений
- Бот будет уведомлять только о ценах ниже порога

### Просмотр маршрутов

**📋 Мои маршруты** → выберите маршрут → подробная информация:
- Лучшие 3 цены с ссылками на покупку
- График цен (динамика изменений)
- Heatmap (лучшее время для покупки)

### Редактирование маршрута

- **Пауза/Возобновить** - временно отключить проверку
- **Изменить порог** - новая пороговая цена
- **Удалить** - полное удаление маршрута

### Настройки

#### 🌙 Тихие часы
Время, когда бот не отправляет уведомления.
- Начало (например, 23:00)
- Конец (например, 07:00)

#### 🌍 Таймзона
Для корректного отображения времени уведомлений.
- Выберите из списка или введите вручную
- Формат: `Europe/Moscow`, `Asia/Tokyo`

### Лимиты (бесплатно)

- **1 гибкий маршрут** (диапазон дат)
- **3 фиксированных маршрута** (конкретные даты)
- **Максимум 20 комбинаций** для гибкого поиска

💎 В будущем будет доступна платная подписка для расширенных возможностей.

---

## 🛠️ Для разработчиков

### Технологии

- **Node.js** - платформа
- **node-telegram-bot-api** - Telegram Bot API
- **SQLite3** - база данных
- **Puppeteer** - веб-скрейпинг
- **node-cron** - планировщик задач
- **chartjs-node-canvas** - генерация графиков

### Структура проекта

```
├── config/
│   ├── constants.js          # Константы (авиакомпании)
│   └── database.js            # Инициализация БД + миграции
├── handlers/
│   ├── routeHandlers.js       # Обработка маршрутов
│   └── settingsHandlers.js    # Обработка настроек
├── models/
│   ├── UnifiedRoute.js        # Модель маршрута
│   └── RouteResult.js         # Модель результата поиска
├── services/
│   ├── UnifiedMonitor.js      # Мониторинг всех маршрутов
│   ├── AviasalesPricer.js     # Парсинг Aviasales
│   ├── ChartGenerator.js      # Генерация графиков
│   └── NotificationService.js # Уведомления + тихие часы
├── utils/
│   ├── dateUtils.js           # Работа с датами
│   ├── timezoneUtils.js       # Работа с таймзонами
│   └── formatters.js          # Форматирование данных
├── migrations/
│   ├── seed_test_data.js      # Тестовые данные
│   └── cleanup_test_data.js   # Очистка тестовых данных
├── index.js                   # Главный файл бота
├── scheduler.js               # Планировщик проверок
├── package.json
└── README.md
```

---

## 🚀 Установка

### 1. Клонирование репозитория

```bash
git clone https://github.com/your-repo/aviasales-bot.git
cd aviasales-bot
```

### 2. Установка зависимостей

```bash
npm install
```

### 3. Настройка окружения

Создайте файл `.env`:

```env
TELEGRAM_BOT_TOKEN=your_telegram_bot_token
TRAVELPAYOUTS_TOKEN=your_travelpayouts_token
```

### 4. Инициализация базы данных

База данных создается автоматически при первом запуске.

Для создания тестовых данных:

```bash
node migrations/seed_test_data.js
```

Для очистки тестовых данных:

```bash
node migrations/cleanup_test_data.js
```

### 5. Запуск бота

```bash
# Основной бот
node index.js

# Планировщик (в отдельном терминале)
node scheduler.js
```

## 🚀 Запуск через PM2

### Установка PM2

```bash
npm install -g pm2
````
#### Команды управления
```bash
# Запуск бота и планировщика
npm run pm2:start

# Остановка всех процессов
npm run pm2:stop

# Перезапуск
npm run pm2:restart

# Удаление из PM2
npm run pm2:delete

# Просмотр логов
npm run pm2:logs

# Мониторинг
npm run pm2:monit

# Статус процессов
pm2 status
````
#### Автозапуск при перезагрузке сервера
```bash
# Сохранить текущий список процессов
pm2 save

# Настроить автозапуск
pm2 startup
````

### Выполнить команду, которую выдаст PM2
#### Просмотр логов конкретного процесса
```bash
# Логи бота
pm2 logs aviasales-bot

# Логи планировщика
pm2 logs aviasales-scheduler

# Только ошибки
pm2 logs --err

# Последние 100 строк
pm2 logs --lines 100
````
---

## 📊 Архитектура

### База данных

#### unified_routes
Хранит все маршруты (фиксированные и гибкие).

| Поле | Тип | Описание |
|------|-----|----------|
| id | INTEGER | ID маршрута |
| chat_id | INTEGER | ID пользователя |
| origin | TEXT | Аэропорт вылета |
| destination | TEXT | Аэропорт назначения |
| is_flexible | INTEGER | 0 = фикс., 1 = гибкий |
| has_return | INTEGER | 0 = в одну сторону, 1 = туда-обратно |
| departure_date | TEXT | Дата вылета (для фикс.) |
| return_date | TEXT | Дата возврата (для фикс.) |
| departure_start | TEXT | Начало диапазона (для гибкого) |
| departure_end | TEXT | Конец диапазона (для гибкого) |
| min_days | INTEGER | Мин. дней в стране (для гибкого) |
| max_days | INTEGER | Макс. дней в стране (для гибкого) |
| adults | INTEGER | Количество взрослых |
| children | INTEGER | Количество детей |
| airline | TEXT | Код авиакомпании |
| baggage | INTEGER | Багаж (0/1) |
| max_stops | INTEGER | Макс. пересадок |
| max_layover_hours | INTEGER | Макс. время пересадки |
| threshold_price | REAL | Пороговая цена |
| currency | TEXT | Валюта |
| is_paused | INTEGER | На паузе (0/1) |
| created_at | DATETIME | Дата создания |
| last_check | DATETIME | Последняя проверка |

#### route_results
Результаты поиска цен.

| Поле | Тип | Описание |
|------|-----|----------|
| id | INTEGER | ID результата |
| route_id | INTEGER | ID маршрута |
| departure_date | TEXT | Дата вылета |
| return_date | TEXT | Дата возврата |
| days_in_country | INTEGER | Дней в стране |
| total_price | REAL | Цена |
| airline | TEXT | Авиакомпания |
| search_link | TEXT | Ссылка на билет |
| screenshot_path | TEXT | Путь к скриншоту |
| found_at | DATETIME | Время нахождения |

#### user_settings
Настройки пользователей.

| Поле | Тип | Описание |
|------|-----|----------|
| chat_id | INTEGER | ID пользователя |
| quiet_hours_start | INTEGER | Начало тихих часов |
| quiet_hours_end | INTEGER | Конец тихих часов |
| timezone | TEXT | Таймзона пользователя |
| created_at | DATETIME | Дата создания |

### Типы маршрутов

#### 1. Фиксированный туда-обратно
```javascript
{
  is_flexible: 0,
  has_return: 1,
  departure_date: '2026-03-15',
  return_date: '2026-03-20'
}
```

#### 2. Фиксированный в одну сторону
```javascript
{
  is_flexible: 0,
  has_return: 0,
  departure_date: '2026-03-15',
  return_date: null
}
```

#### 3. Гибкий туда-обратно
```javascript
{
  is_flexible: 1,
  has_return: 1,
  departure_start: '2026-05-01',
  departure_end: '2026-05-10',
  min_days: 3,
  max_days: 7
}
```
Генерирует комбинации: (10 дат вылета) × (5 вариантов дней) = 50 комбинаций

#### 4. Гибкий в одну сторону
```javascript
{
  is_flexible: 1,
  has_return: 0,
  departure_start: '2026-05-01',
  departure_end: '2026-05-10'
}
```
Генерирует 10 дат вылета

### Работа с таймзонами

- Все даты/время в БД хранятся в **UTC**
- При отображении конвертируются в таймзону пользователя
- Дефолтная таймзона: `Asia/Yekaterinburg`

### Тихие часы

- Работают в **таймзоне пользователя**
- По умолчанию: **23:00 - 07:00**
- Проверяются перед каждым уведомлением

---

## 🔧 API

### UnifiedRoute

```javascript
// Создание маршрута
UnifiedRoute.create(chatId, routeData)

// Поиск маршрута
UnifiedRoute.findById(id)
UnifiedRoute.findByChatId(chatId)
UnifiedRoute.getAllActive()

// Обновление
UnifiedRoute.updatePauseStatus(id, isPaused)
UnifiedRoute.updateThreshold(id, threshold)
UnifiedRoute.updateLastCheck(id)

// Удаление
UnifiedRoute.delete(id)

// Генерация комбинаций
UnifiedRoute.getCombinations(route)
UnifiedRoute.countCombinations(route)
```

### RouteResult

```javascript
// Сохранение результата
RouteResult.save(routeId, resultData)

// Получение результатов
RouteResult.getTopResults(routeId, limit)
RouteResult.getByRouteId(routeId)
RouteResult.getBestPrice(routeId)

// Очистка старых данных
RouteResult.cleanOldResults(routeId, keepCount)
```

---

## 📝 Лицензия

MIT

---

## 👨‍💻 Автор

Ваше имя / Контакт

---

## 🤝 Вклад

Pull requests приветствуются!

---

## 📞 Поддержка

Если у вас есть вопросы или проблемы, создайте issue в репозитории.
```

***

### **18. package.json**

```json
{
  "name": "aviasales-telegram-bot",
  "version": "2.0.0",
  "description": "Telegram bot for monitoring flight prices on Aviasales",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "scheduler": "node scheduler.js",
    "seed": "node migrations/seed_test_data.js",
    "cleanup": "node migrations/cleanup_test_data.js",
    "dev": "nodemon index.js"
  },
  "keywords": [
    "telegram",
    "bot",
    "aviasales",
    "flight",
    "price",
    "monitor"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "node-telegram-bot-api": "^0.61.0",
    "sqlite3": "^5.1.6",
    "puppeteer": "^21.0.0",
    "node-cron": "^3.0.2",
    "dotenv": "^16.0.3",
    "chartjs-node-canvas": "^4.1.6"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
```