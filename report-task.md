# Рефакторинг системы уведомлений

## Обзор

Полная переработка системы уведомлений: новый формат сообщений (одно сообщение вместо нескольких), приоритетная модель оповещений (CRITICAL/HIGH/MEDIUM/LOW), дайджесты, ночной режим, отображение городов вместо кодов аэропортов.

---

## Файлы для изменения

| Файл | Действие | Описание |
|------|----------|----------|
| `utils/AirportCodeResolver.js` | **СОЗДАТЬ** | Кэш IATA → название города |
| `config/database.js` | ИЗМЕНИТЬ | Новые таблицы + колонки в user_settings |
| `services/NotificationService.js` | ПЕРЕПИСАТЬ | Приоритеты, новый формат, дайджест |
| `scheduler.js` | ИЗМЕНИТЬ | Новый flow проверки + крон дайджеста |
| `handlers/settingsHandlers.js` | ИЗМЕНИТЬ | Новые настройки уведомлений |
| `services/UnifiedMonitor.js` | ИЗМЕНИТЬ | Убрать вызов sendPriceAlert (строки 122-136) |

---

## Порядок реализации

### Шаг 1: Миграция БД (`config/database.js`)

Добавить в `db.serialize()`:

**Таблица `notification_log`** — история отправленных уведомлений:
```sql
CREATE TABLE IF NOT EXISTS notification_log (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  chat_id INTEGER NOT NULL,
  route_id INTEGER,
  priority TEXT NOT NULL,        -- 'CRITICAL', 'HIGH', 'MEDIUM', 'LOW'
  price REAL,
  message_type TEXT NOT NULL,    -- 'instant', 'digest', 'report'
  sent_at DATETIME DEFAULT (datetime('now')),
  disable_notification INTEGER DEFAULT 0
);
CREATE INDEX IF NOT EXISTS idx_notif_log_chat_priority ON notification_log(chat_id, priority, sent_at);
CREATE INDEX IF NOT EXISTS idx_notif_log_route ON notification_log(route_id, priority, sent_at);
```

**Таблица `daily_digest_queue`** — очередь для дайджеста:
```sql
CREATE TABLE IF NOT EXISTS daily_digest_queue (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  chat_id INTEGER NOT NULL,
  route_id INTEGER NOT NULL,
  priority TEXT NOT NULL,
  price REAL NOT NULL,
  avg_price REAL,
  historical_min REAL,
  best_result_id INTEGER,
  created_at DATETIME DEFAULT (datetime('now')),
  processed INTEGER DEFAULT 0
);
CREATE INDEX IF NOT EXISTS idx_digest_queue_chat ON daily_digest_queue(chat_id, processed);
```

**Новые колонки в `user_settings`** (через ALTER TABLE с try/catch как в существующих миграциях строки 379-394):
```sql
ALTER TABLE user_settings ADD COLUMN night_mode INTEGER DEFAULT 1;
ALTER TABLE user_settings ADD COLUMN notifications_enabled INTEGER DEFAULT 1;
ALTER TABLE user_settings ADD COLUMN digest_enabled INTEGER DEFAULT 1;
```

**Миграция значения `notify_on_check`**: после добавления новых колонок, мигрировать старое значение:
```sql
UPDATE user_settings
SET notifications_enabled = notify_on_check,
    digest_enabled = notify_on_check
WHERE notify_on_check = 1;
```
Поле `notify_on_check` останется в таблице для совместимости, но в новом коде использоваться не будет. Из UI оно убирается.

---

### Шаг 2: AirportCodeResolver (`utils/AirportCodeResolver.js`) — НОВЫЙ ФАЙЛ

Синглтон, загружает все аэропорты из таблицы `airports` в Map при первом вызове `load()`.

**Методы:**
- `async load()` — загрузка из БД (идемпотентная, `SELECT iata_code, city_name, city_name_en, country_name FROM airports GROUP BY iata_code`)
- `getCityName(iataCode)` — возвращает русское название города (или сам код если не найден)
- `formatRoute(origin, destination)` — возвращает "Москва → Стамбул"

Синхронные геттеры после загрузки — никаких async при форматировании сообщений.

**Переиспользование:** класс будет использоваться в NotificationService, админке и других местах где нужно отображать города вместо кодов.

---

### Шаг 3: Переработка NotificationService (`services/NotificationService.js`)

**Переиспользуем из существующего кода:**
- `utils/formatters.js`: `Formatters.formatPrice()`, `Formatters.getAirlineName()`, `Formatters._pluralize()`, `Formatters._formatShortDate()`
- `config/constants.js`: `AIRLINES` маппинг
- Существующие методы: `_canSendNotification()`, `_getUserTimezone()`, `_formatDateTimeForUser()`, `_pluralizeDays()`

#### 3a. Новый метод: `classifyPriority(routeData)`

```
Вход: { currentPrice, userBudget, avgPrice, historicalMin, priceDropPercent }
Выход: { priority: 'CRITICAL'|'HIGH'|'MEDIUM'|'LOW', reasons: string[] }
```

Логика (из report-periods.md):
- **CRITICAL**: цена <= бюджет, ИЛИ исторический минимум, ИЛИ скидка 50%+ от средней
- **HIGH**: превышение бюджета 10-15%, ИЛИ скидка 30-50% от средней, ИЛИ падение 15%+ за 24ч
- **MEDIUM**: превышение 15-30%, ИЛИ скидка 15-30%, ИЛИ падение 10-15%
- **LOW**: всё остальное

#### 3b. Вспомогательные методы аналитики

- `getRouteAnalytics(routeId)` — `SELECT AVG(price), MIN(price), COUNT(*) FROM price_analytics WHERE route_id = ?` (индекс `idx_price_analytics_route_id` уже существует)
- `getPriceDropPercent(routeId, currentPrice)` — сравнение с минимумом 24-48ч назад
- `getRouteCheckStats(routeId)` — текущая проверка + сумма всех исторических комбинаций из `route_check_stats`

#### 3c. Методы маршрутизации уведомлений

- `_checkPriorityCooldown(chatId, routeId, priority, hours)` — проверка по `notification_log`
- `_getCriticalCountToday(chatId)` — COUNT из `notification_log` за сегодня
- `_logNotification(chatId, routeId, priority, price, type, silent)` — INSERT в `notification_log`
- `_addToDigestQueue(chatId, routeId, priority, price, analytics)` — INSERT в `daily_digest_queue`
- `_isNightTime(timezone, settings)` — проверка 23:00-08:00 в таймзоне юзера

#### 3d. Метод `processAndRouteNotification(...)` — центральный роутер

Логика для каждого приоритета:

| Приоритет | Звук | Игнорирует "Выкл"? | Free | Plus |
|-----------|------|---------------------|------|------|
| CRITICAL | Да (ночью нет) | Да | Макс 3/день, потом в дайджест | Без лимита |
| HIGH | Нет | Нет | Только дайджест | Раз в 3ч, потом дайджест |
| MEDIUM | Нет | Нет | Дайджест | Дайджест |
| LOW | — | — | Тихо | Тихо |

Ночной режим: CRITICAL — беззвучно, остальные — откладываются до утра (в дайджест).

#### 3e. Метод `formatSingleRouteBlock(route, bestResult, analytics, checkStats)`

Формирует блок одного маршрута. Формат:

```
🎉 Москва → Стамбул
💰 11 900₽ — на 6 100₽ ниже бюджета!

Ваш бюджет: 18 000₽
Средняя цена: 22 400₽ • Экономия: 10 500₽ (47%)
🔥 Исторический минимум за всё время!

📅 20 марта-27 марта • Turkish Airlines
2 взр • Багаж • Прямой

Сейчас выполнено 38 проверок из 1 847 проверок всего
```

Детали:
- `AirportCodeResolver.formatRoute()` для городов
- `Formatters.formatPrice()` для цен
- `Formatters.getAirlineName()` для авиакомпании
- Эмодзи: `🎉` если сильно ниже бюджета, `✅` если в бюджете, `📉` если падает, `📊` обычная проверка
- Бюджет: "Ваш бюджет: X₽" + контекст:
  - Ниже бюджета: "на Y₽ ниже бюджета!"
  - В бюджете: "В рамках бюджета! ✅"
  - Выше бюджета: "Превышение: Y₽, продолжаем искать"
- Средняя цена из `price_analytics` (показывать только при >= 5 data points)
- Комбинации: "Сейчас выполнено X проверок из Y проверок всего" (только для гибких)
- Для обычных: "Всего выполнено Y проверок"
- Детали билета: даты, авиакомпания, пассажиры, багаж, пересадки (короткой строкой)

#### 3f. Метод `sendConsolidatedReport(chatId, routeBlocks, timezone, disableNotification)`

Собирает все блоки в одно сообщение. Если > 4000 символов — разбивает на несколько (лимит Telegram 4096). Inline кнопки "Купить билет →" / "Смотреть →" привязаны к маршрутам.

Заголовок зависит от содержания:
- Есть CRITICAL: "🚨 Отличные новости! • HH:MM"
- Обычная проверка: "📊 Проверка завершена • HH:MM"

Подвал:
- Есть находки: "Отличные цены! Не упусти 🎯"
- Нет находок: "Продолжаю мониторинг 🔍"

Разделитель между маршрутами: `━━━━━━━━━━━━━━━━━━━━━━━`

#### 3g. Удалить/переработать старые методы

- `sendCheckReport` — заменяется на `sendConsolidatedReport`
- `sendPriceAlert` — логика переносится в `processAndRouteNotification` + `sendConsolidatedReport`
- `getUserRoutesStats` — переработать для нового формата (или заменить прямыми запросами)
- `sendBroadcastMessages` — оставить без изменений
- `recordNotification` — заменить на `_logNotification`

---

### Шаг 4: Переработка scheduler.js

#### 4a. Изменить `checkUserRoutes()`

Новый flow:
1. Загрузить `AirportCodeResolver.load()`
2. Получить `userSettings` и `subscription` для пользователя
3. Для каждого маршрута:
   - `monitor.checkSingleRoute(route)` (данные сохраняются в БД)
   - `RouteResult.getTopResults(route.id, 1)` — лучший билет
   - `notificationService.getRouteAnalytics(route.id)` — средняя/мин цена
   - `notificationService.getRouteCheckStats(route.id)` — статистика комбинаций
   - `notificationService.processAndRouteNotification(...)` — классификация и маршрутизация
   - `notificationService.formatSingleRouteBlock(...)` — формат блока
   - Если CRITICAL/HIGH и решение "отправить" — немедленная отправка одиночного алерта
4. После всех маршрутов: если `notifications_enabled` — отправить сводный отчёт (если не ночь и не все уже отправлены как CRITICAL)

#### 4b. Добавить крон для дайджеста

```javascript
// Каждый час проверяем, кому пора отправить дайджест
cron.schedule('0 * * * *', async () => {
  await sendDigestsForCurrentHour();
});
```

`sendDigestsForCurrentHour()`:
1. Получить пользователей с непустой очередью дайджеста (`daily_digest_queue WHERE processed = 0`)
2. Для каждого: проверить локальное время (через timezone)
3. Free: отправлять в 10:00 локальное
4. Plus: отправлять в 10:00 и 18:00 локальное
5. Собрать блоки из очереди, отправить через `sendConsolidatedReport`, пометить `processed = 1`

#### 4c. Новые вспомогательные функции

- `getSubscriptionForUser(chatId)` — JOIN user_subscriptions + subscription_types
- `getUsersWithPendingDigest()` — SELECT DISTINCT из daily_digest_queue
- `getPendingDigestItems(chatId)` — все непроцессированные записи
- `markDigestProcessed(chatId)` — UPDATE processed = 1

---

### Шаг 5: Убрать sendPriceAlert из UnifiedMonitor (`services/UnifiedMonitor.js`)

Удалить строки 122-136 (блок `if (route.threshold_price && priceResult.price <= route.threshold_price) { ... sendPriceAlert ... }`). Уведомления теперь маршрутизируются через scheduler → NotificationService.processAndRouteNotification.

Оставить без изменений: сохранение в `route_results`, `price_analytics`, `route_check_stats`, `combination_check_results`.

---

### Шаг 6: Обновление настроек (`handlers/settingsHandlers.js`)

#### Новое меню настроек:
```
🌙 Тихие часы
🌍 Таймзона
🔔 Уведомления        ← переименовано + новая логика
🌙 Ночной режим        ← новый пункт
📊 Дайджест            ← новый пункт
🏠 Главное меню
```

#### Экран "Уведомления":
```
🔔 УВЕДОМЛЕНИЯ

Текущий статус: ✅ Включены

💡 Даже при отключении вы получите уведомление, когда:
• Цена в рамках вашего бюджета
• Исторический минимум цены
• Супер-скидка 50%+

Эти находки исчезают быстро — важно не упустить!
```
Кнопки: [🔔 Включить / 🔕 Отключить], [◀️ Назад]

#### Экран "Ночной режим":
```
🌙 НОЧНОЙ РЕЖИМ

Текущий статус: 🌙 Включён (23:00-08:00)

При включённом ночном режиме (23:00-08:00):
• Критические находки приходят беззвучно
• Остальные уведомления откладываются до утра
```
Кнопки: [🌙 Включить / 🔕 Отключить], [◀️ Назад]

#### Экран "Дайджест":
```
📊 ДАЙДЖЕСТ

Ежедневная сводка по всем маршрутам.
Бесплатная подписка: 1 раз/день (10:00)
Plus: 2 раза/день (10:00 и 18:00)

Текущий статус: ✅ Включён
```
Кнопки: [📊 Включить / 📊 Отключить], [◀️ Назад]

#### Обработка состояний в `handleMessage()`:
- Заменить обработку `🔔 Уведомления о проверках` на `🔔 Уведомления`
- Новые шаги: `notifications_menu`, `night_mode_menu`, `digest_menu`
- Новые DB-хелперы: `_updateNightMode`, `_updateNotificationsEnabled`, `_updateDigestEnabled`
- Обновить `_getUserSettings` default values (строки 598-609): добавить `notifications_enabled: 1, night_mode: 1, digest_enabled: 1`

---

## Проверка после реализации

1. **Миграция БД**: запустить бота, проверить что новые таблицы и колонки созданы без ошибок
2. **AirportCodeResolver**: проверить `getCityName('MOW')` → 'Москва', `getCityName('DPS')` → 'Денпасар'
3. **Формат сообщений**: запустить ручную проверку (`/admin_check`), убедиться что приходит одно сообщение с городами, бюджетом, средней ценой, кнопкой
4. **Приоритеты**: создать маршрут с порогом выше текущей цены → должен прийти CRITICAL. Маршрут с порогом сильно ниже → LOW, не приходит
5. **Дайджест**: добавить элемент в очередь, подождать наступления часа дайджеста
6. **Настройки**: проверить все три новых раздела в меню настроек бота
7. **Ночной режим**: проверить что CRITICAL приходит беззвучно, а HIGH/MEDIUM откладываются
8. **4096 лимит**: создать 5+ маршрутов у одного пользователя, проверить разбиение сообщения
