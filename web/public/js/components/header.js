// web/public/js/components/header.js

class Header {
    render() {
        // Header is already in HTML, just add any dynamic functionality if needed
        console.log('Header initialized');
    }
}

const header = new Header();
export default header;
