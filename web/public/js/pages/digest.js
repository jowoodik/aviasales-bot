// web/public/js/pages/digest.js

/**
 * Страница очереди дайджестов (УДАЛЕНА)
 *
 * Дайджест-система была удалена из бота в рамках миграции на новую систему уведомлений.
 * Теперь все уведомления отправляются немедленно на основе приоритетов:
 * - CRITICAL (цена < бюджет): раз в 6ч или при падении цены
 * - HIGH (исторический минимум): раз в 12ч
 * - LOW (остальное): раз в 24ч
 *
 * См. документацию: /docs/notification-system.md
 */

class DigestQueuePage {
    constructor() {
        this.queue = [];
    }

    async render() {
        const content = document.getElementById('main-content');
        if (!content) return;

        content.innerHTML = `
            <div class="container-fluid">
                <div class="row mb-4">
                    <div class="col">
                        <h2>
                            <i class="bi bi-inbox"></i>
                            Очередь дайджеста
                        </h2>
                    </div>
                </div>

                <div class="alert alert-info">
                    <h4><i class="bi bi-info-circle"></i> Функция удалена</h4>
                    <p class="mb-0">
                        <strong>Дайджест-система была удалена</strong> в рамках миграции на новую систему уведомлений.
                        <br><br>
                        Теперь все уведомления отправляются немедленно на основе приоритетов:
                    </p>
                    <ul class="mt-3 mb-3">
                        <li><strong>🚨 CRITICAL</strong> (цена &lt; бюджет): раз в 6ч или при падении цены</li>
                        <li><strong>🔥 HIGH</strong> (исторический минимум): раз в 12ч</li>
                        <li><strong>📊 LOW</strong> (остальное): раз в 24ч</li>
                    </ul>
                    <p class="mb-0">
                        Для просмотра отправленных уведомлений используйте <a href="#notifications" class="alert-link">Лог уведомлений</a>.
                    </p>
                </div>
            </div>
        `;
    }

    destroy() {
        // Ничего не делаем
    }
}

export default DigestQueuePage;
