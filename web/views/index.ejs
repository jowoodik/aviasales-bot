<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Aviasales Price Monitor Bot</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: #0f0f23;
            color: #e0e0e0;
            display: flex;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            padding: 20px;
        }

        .container {
            max-width: 600px;
            width: 100%;
            text-align: center;
        }

        .logo {
            font-size: 5em;
            margin-bottom: 20px;
            animation: pulse 2s infinite;
        }

        @keyframes pulse {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.05); }
        }

        h1 {
            font-size: 2.5em;
            margin-bottom: 15px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .subtitle {
            font-size: 1.2em;
            color: #a0a0a0;
            margin-bottom: 40px;
        }

        .card {
            background: #1a1a2e;
            border-radius: 15px;
            padding: 30px;
            margin-bottom: 30px;
            border: 1px solid #2d2d44;
            box-shadow: 0 4px 20px rgba(0,0,0,0.3);
        }

        .input-group {
            display: flex;
            gap: 10px;
            margin-bottom: 20px;
        }

        input {
            flex: 1;
            padding: 15px;
            background: #16213e;
            border: 1px solid #2d2d44;
            border-radius: 8px;
            color: #e0e0e0;
            font-size: 1em;
            transition: all 0.3s;
        }

        input:focus {
            outline: none;
            border-color: #667eea;
            box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.2);
        }

        input::placeholder {
            color: #666;
        }

        .btn {
            padding: 15px 30px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            border: none;
            border-radius: 8px;
            color: white;
            font-size: 1em;
            font-weight: bold;
            cursor: pointer;
            transition: all 0.3s;
            display: inline-flex;
            align-items: center;
            gap: 10px;
        }

        .btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 20px rgba(102, 126, 234, 0.4);
        }

        .features {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
            gap: 20px;
            margin-top: 30px;
        }

        .feature {
            padding: 20px;
            background: #16213e;
            border-radius: 10px;
            border: 1px solid #2d2d44;
            transition: all 0.3s;
        }

        .feature:hover {
            border-color: #667eea;
            transform: translateY(-3px);
        }

        .feature-icon {
            font-size: 2.5em;
            margin-bottom: 10px;
        }

        .feature-title {
            font-weight: bold;
            margin-bottom: 5px;
            color: #e0e0e0;
        }

        .feature-desc {
            font-size: 0.85em;
            color: #a0a0a0;
        }

        .links {
            display: flex;
            gap: 15px;
            justify-content: center;
            margin-top: 30px;
        }

        .link {
            color: #667eea;
            text-decoration: none;
            transition: all 0.3s;
            display: flex;
            align-items: center;
            gap: 5px;
        }

        .link:hover {
            color: #764ba2;
        }

        @media (max-width: 768px) {
            h1 {
                font-size: 2em;
            }
            .logo {
                font-size: 3em;
            }
            .features {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="logo">✈️</div>
        <h1>Aviasales Price Monitor</h1>
        <p class="subtitle">Telegram-бот для отслеживания цен на авиабилеты</p>

        <div class="card">
            <h2 style="margin-bottom: 20px; color: #e0e0e0;">🚀 Войти в Dashboard</h2>
            <div class="input-group">
                <input type="text" id="chatId" placeholder="Введите ваш Chat ID" />
            </div>
            <button class="btn" onclick="goToDashboard()">
                <i class="fas fa-sign-in-alt"></i>
                Открыть Dashboard
            </button>
            <p style="margin-top: 15px; font-size: 0.85em; color: #666;">
                💡 Chat ID можно узнать в боте командой /start
            </p>
        </div>

        <div class="features">
            <div class="feature">
                <div class="feature-icon">📊</div>
                <div class="feature-title">Мониторинг цен</div>
                <div class="feature-desc">Автоматическое отслеживание</div>
            </div>
            <div class="feature">
                <div class="feature-icon">🔔</div>
                <div class="feature-title">Уведомления</div>
                <div class="feature-desc">Мгновенные оповещения</div>
            </div>
            <div class="feature">
                <div class="feature-icon">📈</div>
                <div class="feature-title">Аналитика</div>
                <div class="feature-desc">Графики и статистика</div>
            </div>
            <div class="feature">
                <div class="feature-icon">🔍</div>
                <div class="feature-title">Гибкий поиск</div>
                <div class="feature-desc">Диапазон дат</div>
            </div>
        </div>

        <div class="links">
            <a href="https://t.me/YOUR_BOT_USERNAME" class="link" target="_blank">
                <i class="fab fa-telegram"></i>
                Открыть бота
            </a>
            <a href="https://github.com/YOUR_REPO" class="link" target="_blank">
                <i class="fab fa-github"></i>
                GitHub
            </a>
            <a href="#" class="link" onclick="showHelp()">
                <i class="fas fa-question-circle"></i>
                Помощь
            </a>
        </div>
    </div>

    <script>
        function goToDashboard() {
            const chatId = document.getElementById('chatId').value.trim();
            if (!chatId) {
                alert('Пожалуйста, введите ваш Chat ID');
                return;
            }
            if (isNaN(chatId)) {
                alert('Chat ID должен быть числом');
                return;
            }
            window.location.href = `/dashboard?chat_id=${chatId}`;
        }

        // Enter для перехода
        document.getElementById('chatId').addEventListener('keypress', function(e) {
            if (e.key === 'Enter') {
                goToDashboard();
            }
        });

        function showHelp() {
            alert(`📖 Как использовать:

1. Запустите бота в Telegram
2. Узнайте ваш Chat ID командой /start
3. Введите Chat ID на этой странице
4. Откройте Dashboard

🔹 В Dashboard вы сможете:
  • Просматривать все маршруты
  • Смотреть графики цен
  • Управлять настройками
  • Видеть лучшие предложения`);
        }
    </script>
</body>
</html>
